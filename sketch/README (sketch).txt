# Compile With:

`make`

# Run With:

`./sketch` to run tests

or 

`./sketch NAME.sketch` to draw a specific sketch







# Design 

To Handle changes in colour (lawn test), I used the variables opcodeP and operandP which remember the previous state of opcode and operand. This allowed me to perform the operations in the correct order.